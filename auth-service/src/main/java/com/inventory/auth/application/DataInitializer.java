package com.inventory.auth.application;

// DataInitializer removed — users must sign up via /api/auth/signup
// No default accounts are seeded. First user to sign up with role ADMIN becomes the system admin.
